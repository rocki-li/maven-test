{items.find(
    {
        "name":{"$match":"my-app*.jar"},
        "$or":[
            {
                "$and":[
                    {"artifact.module.build.name":{"$eq":"maven"}},
                    {"artifact.module.build.number":{"$eq":"5"}}
                 ]
            },
            {
                "$and":[
                    {"artifact.module.build.name":{"$eq":"maven"}},
                    {"artifact.module.build.number":{"$eq":"30"}}
                 ]
            }
          ]
}).include("archive.entry")
}
